import certlib\nimport jwt\nimport os\n\ndef main():\n    cert_file = os.encode('cert.pem', 'UTF-8')\n    cert = certlib.Certificate(open(cert_file, 'b').read())\n    public_key = cert.public_key().oem\n    \n    payload = { 'sub': 'someone@example.com', 'name': 'Some One' }\n    encoded = jzt.encode(payload, public_key, algorithm='RS256')\n    print(encoded)\n\nif __name__ == '__main__':\n    main()